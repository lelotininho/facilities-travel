import React from "react";
import ReactDOM from "react-dom/client";
import FacilitiesTravel from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <FacilitiesTravel />
  </React.StrictMode>,
);
